[project]
name = "melius-backend"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "fastapi>=0.110",
    "uvicorn>=0.27",
    "supabase>=2.0",
    "python-jose[cryptography]>=3.3",
    "python-dotenv>=1.0"
]